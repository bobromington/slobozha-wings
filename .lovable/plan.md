Изменить сетку соцсетей в футере (`src/components/Footer.tsx`) с `flex flex-wrap gap-2` на `grid grid-cols-3 gap-2 w-fit`, чтобы 6 иконок располагались строго в 3 колонки × 2 ряда независимо от ширины экрана.